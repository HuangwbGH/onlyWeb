# U8 104账套 采购订单 WF-SPP260408-1 审核前后差异

- 数据库：`UFDATA_104_2018`
- 单据号：`WF-SPP260408-1`
- 对比时间：`2026-04-10 16:24 CST`
- 对比基线：`archive/吉信德/U8-104-采购订单-WF-SPP260408-1-审核前字段快照.md`

## 一、结论

本次审核后，**表头主表 `PO_Pomain` 发生了字段变化**；
**表头扩展表 `PO_Pomain_extradefine` 未发生变化**；
**表体明细表 `PO_Podetails` 当前未检测到字段值变化**；
但**历史表新增了审核前快照记录**：

- `PO_PomainHistory`：新增 `1` 条
- `PO_PodetailsHistory`：新增 `3` 条

这说明本次审核动作的主要影响集中在：
1. 表头审批状态字段更新
2. 历史表留痕
3. 表体当前行值本身未直接改动

---

## 二、表头 `PO_Pomain` 变化字段

### 1. `cAuditDate`
- 审核前：`null`
- 审核后：`2026-04-10 00:00:00`

### 2. `cAuditTime`
- 审核前：`null`
- 审核后：`2026-04-10 16:16:42.253000`

### 3. `cState`
- 审核前：`0`
- 审核后：`1`

### 4. `cVerifier`
- 审核前：`null`
- 审核后：`管理员`

### 5. `iverifystateex`
- 审核前：`null`
- 审核后：`2`

### 6. `ufts`
- 审核前：`b'\x00\x00\x00\x00\t7R\xad'`
- 审核后：`b'\x00\x00\x00\x00\t7\xae\xdf'`

---

## 三、表头扩展表 `PO_Pomain_extradefine`

未检测到字段变化。

---

## 四、表体 `PO_Podetails`

本次对 3 行明细逐字段比较，**未检测到字段值变化**。

也就是说，下列明细记录当前值与审核前一致：
- `ID = 1000047645`
- `ID = 1000047646`
- `ID = 1000047647`

说明本次审核动作**没有直接改写当前表体明细字段值**。

---

## 五、历史表变化

### 1. `PO_PomainHistory`
- 审核前：`0` 条
- 审核后：`1` 条

### 2. `PO_PodetailsHistory`
- 审核前：`0` 条
- 审核后：`3` 条

说明 U8 在本次审核时，已将：
- 表头旧状态写入 `PO_PomainHistory`
- 3 行表体旧状态写入 `PO_PodetailsHistory`

这点很重要，后续如果要继续逆向“正式审核”机制，历史表是关键证据。

---

## 六、当前审核后表头关键信息

```json
{
  "cPOID": "WF-SPP260408-1",
  "POID": 1000012819,
  "cState": 1,
  "cVerifier": "管理员",
  "cAuditDate": "2026-04-10 00:00:00",
  "cAuditTime": "2026-04-10 16:16:42.253000",
  "iverifystateex": 2,
  "IsWfControlled": false,
  "iflowid": null,
  "cCurrentAuditor": null,
  "cMaker": "demo",
  "ufts": "b'\\x00\\x00\\x00\\x00\\t7\\xae\\xdf'"
}
```

---

## 七、当前可归纳出的采购订单“审核动作直接影响字段”

### 表头直接变化字段
- `cState`
- `cVerifier`
- `cAuditDate`
- `cAuditTime`
- `iverifystateex`
- `ufts`

### 表头未变化但值得持续观察
- `iflowid`
- `cCurrentAuditor`
- `IsWfControlled`
- `cChanger`
- `cModifyDate`
- `cModifyTime`

### 表体当前值未变化
- `PO_Podetails` 全部已对比字段

### 历史表新增
- `PO_PomainHistory`
- `PO_PodetailsHistory`

---

## 八、业务含义解释

本次 `WF-SPP260408-1` 的审核结果可以解释为：

1. **单据状态从未审核转为已审核**
   - `cState: 0 -> 1`
   - `iverifystateex: null -> 2`

2. **审核人和审核时间被正式写入主表**
   - `cVerifier = 管理员`
   - `cAuditDate / cAuditTime` 被回填

3. **表体业务明细未被审批动作改写**
   - 数量、单价、金额、到货日期等保持不变

4. **系统保留了历史痕迹**
   - 历史表新增记录，说明审核动作不是简单改主表，而是带留痕机制
