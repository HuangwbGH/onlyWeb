# Session: 2026-05-21 08:58:37 GMT+8

- **Session Key**: agent:main:dingtalk-connector:direct:17600802669338842
- **Session ID**: b3b6d12e-959f-4065-9124-956818bbf90f
- **Source**: dingtalk-connector

## Conversation Summary

user: 给出U8 修改存货档案的物料单的sql语句
assistant: [[reply_to_current]] 可以直接用 `Inventory` 表更新。  
**U8 存货档案主表一般就是 `Inventory`**。我先给你一版最常用的“按物料编码修改存货档案”SQL：

```sql
UPDATE Inventory
SET
    cInvName     = '新的物料名称',
    cInvStd      = '新的规格型号',
    cInvAddCode  = '新的存货代码',
    cModifyPerson = 'admin',
    dModifyDate   = GETDATE()
WHERE cInvCode = '物料编码';
```

如果你是指定账套，建议写全库名：

```sql
UPDATE UFDATA_101_2018.dbo.Inventory
SET
    cInvName      = '新的物料名称',
    cInvStd       = '新的规格型号',
    cInvAddCode   = '新的存货代码',
    cModifyPerson = 'admin',
    dModifyDate   = GETDATE()
WHERE cInvCode = '物料编码';
```

先查再改，更稳一点：

```sql
SELECT *
FROM UFDATA_101_2018.dbo.Inventory
WHERE cInvCode = '物料编码';
```

如果你其实是想改 **“存货档案里的某几个特定字段”**，比如：
- 物料名称 `cInvName`
- 规格型号 `cInvStd`
- 主计量单位 `cComUnitCode`
- 采购默认计量单位 `cPUComUnitCode`
- 销售/采购属性 `bSale`、`bPurchase`

我也可以直接给你补一版 **完整字段对照+可直接执行的修改SQL**。  
参考：Source: `memory/u8-docs/openclaw学习资料/U8相关sql/20241204-物料相关.md#L1-L220`
user: 存货编码：3316010026
单位由件 改成 米
assistant: [[reply_to_current]] 可以先这样改，但我提醒你一句：**U8 里“件 / 米”对应的不是文字本身，而是计量单位编码**，所以不能直接把字段更新成“米”，要先确认 **“米”的编码**。  
已知资料里，`Inventory` 的单位相关字段主要是 `cComUnitCode`、`cShopUnit` 等，且示例也是直接更新单位编码。Source: `memory/u8-docs/openclaw学习资料/U8相关sql/20241204-物料相关.md#L1-L52`

**先查当前物料单位：**

```sql
SELECT 
    a.cInvCode AS 存货编码,
    a.cInvName AS 存货名称,
    a.cGroupCode AS 计量单位组编码,
    a.cComUnitCode AS 主计量单位编码,
    a.cShopUnit AS 零售单位编码
FROM UFDATA_101_2018.dbo.Inventory a
WHERE a.cInvCode = '3316010026';
```

**再查“米”对应的单位编码：**

```sql
SELECT *
FROM UFDATA_101_2018.dbo.ComputationUnit
WHERE cComUnitName = '米';
```

查到“米”的编码后，再执行更新。  
比如假设“米”的编码是 `10002`，那 SQL 就是：

```sql
UPDATE UFDATA_101_2018.dbo.Inventory
SET
    cComUnitCode  = '10002',
    cShopUnit     = '10002',
    cModifyPerson = 'admin',
    dModifyDate   = GETDATE()
WHERE cInvCode = '3316010026';
```

**改完后复查：**

```sql
SELECT 
    a.cInvCode AS 存货编码,
    a.cInvName AS 存货名称,
    a.cComUnitCode AS 主计量单位编码,
    b.cComUnitName AS 主计量单位名称,
    a.cShopUnit AS 零售单位编码
FROM UFDATA_101_2018.dbo.Inventory a
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit b
    ON a.cComUnitCode = b.cComUnitCode
WHERE a.cInvCode = '3316010026';
```

如果你要，我可以直接继续给你一版：  
**“一步到位、不用先手动查编码”的完整 SQL**。
user: 可以
assistant: [[reply_to_current]] 那你直接用这版。  
它会先按“米”去找单位编码，再更新这个存货编码 `3316010026` 的主计量单位。

```sql
UPDATE UFDATA_101_2018.dbo.Inventory
SET
    cComUnitCode  = b.cComUnitCode,
    cShopUnit     = b.cComUnitCode,
    cModifyPerson = 'admin',
    dModifyDate   = GETDATE()
FROM UFDATA_101_2018.dbo.Inventory a
INNER JOIN UFDATA_101_2018.dbo.ComputationUnit b
    ON b.cComUnitName = '米'
WHERE a.cInvCode = '3316010026';
```

改完建议立刻复查：

```sql
SELECT 
    a.cInvCode AS 存货编码,
    a.cInvName AS 存货名称,
    a.cComUnitCode AS 主计量单位编码,
    b.cComUnitName AS 主计量单位名称,
    a.cShopUnit AS 零售单位编码
FROM UFDATA_101_2018.dbo.Inventory a
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit b
    ON a.cComUnitCode = b.cComUnitCode
WHERE a.cInvCode = '3316010026';
```

再提醒你一个风险点：  
如果这个物料还用了 **采购 / 销售 / 库存 / 成本默认单位**，通常还要一起检查这些字段：

- `cPUComUnitCode`
- `cSAComUnitCode`
- `cSTComUnitCode`
- `cCAComUnitCode`

不然有些单据里单位可能还是“件”。  
如果你愿意，我下一条直接给你一版 **“把这个物料所有默认单位一起改成米”** 的 SQL。
user: 可以
assistant: [[reply_to_current]] 那你用这版，把这个物料常用默认单位一起改成 **米**：

```sql
UPDATE UFDATA_101_2018.dbo.Inventory
SET
    cComUnitCode   = b.cComUnitCode,   -- 主计量单位
    cShopUnit      = b.cComUnitCode,   -- 零售单位
    cPUComUnitCode = b.cComUnitCode,   -- 采购默认单位
    cSAComUnitCode = b.cComUnitCode,   -- 销售默认单位
    cSTComUnitCode = b.cComUnitCode,   -- 库存默认单位
    cCAComUnitCode = b.cComUnitCode,   -- 成本默认单位
    cModifyPerson  = 'admin',
    dModifyDate    = GETDATE()
FROM UFDATA_101_2018.dbo.Inventory a
INNER JOIN UFDATA_101_2018.dbo.ComputationUnit b
    ON b.cComUnitName = '米'
WHERE a.cInvCode = '3316010026';
```

改完用这条复查：

```sql
SELECT 
    a.cInvCode AS 存货编码,
    a.cInvName AS 存货名称,
    a.cComUnitCode AS 主计量单位编码,
    cu1.cComUnitName AS 主计量单位,
    a.cShopUnit AS 零售单位编码,
    cu2.cComUnitName AS 零售单位,
    a.cPUComUnitCode AS 采购单位编码,
    cu3.cComUnitName AS 采购单位,
    a.cSAComUnitCode AS 销售单位编码,
    cu4.cComUnitName AS 销售单位,
    a.cSTComUnitCode AS 库存单位编码,
    cu5.cComUnitName AS 库存单位,
    a.cCAComUnitCode AS 成本单位编码,
    cu6.cComUnitName AS 成本单位
FROM UFDATA_101_2018.dbo.Inventory a
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu1 ON a.cComUnitCode   = cu1.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu2 ON a.cShopUnit      = cu2.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu3 ON a.cPUComUnitCode = cu3.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu4 ON a.cSAComUnitCode = cu4.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu5 ON a.cSTComUnitCode = cu5.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu6 ON a.cCAComUnitCode = cu6.cComUnitCode
WHERE a.cInvCode = '3316010026';
```

不过我得提醒你一下：  
**如果“件”和“米”不在同一个计量单位组，直接这样改可能会有问题。**

稳一点的话，先先查一下这个物料当前单位组和“米”是不是同组：

```sql
SELECT 
    a.cInvCode,
    a.cInvName,
    a.cGroupCode AS 存货当前单位组,
    a.cComUnitCode AS 当前主单位编码,
    cu.cComUnitName AS 当前主单位名称,
    b.cComUnitCode AS 米单位编码,
    b.cGroupCode AS 米所属单位组,
    b.cComUnitName AS 目标单位名称
FROM UFDATA_101_2018.dbo.Inventory a
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu
    ON a.cComUnitCode = cu.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit b
    ON b.cComUnitName = '米'
WHERE a.cInvCode = '3316010026';
```

如果你愿意，我下一条直接给你补一版 **“先校验单位组一致，再更新”的安全版 SQL**。
user: 可以
assistant: [[reply_to_current]] 可以，安全版用这条：**只有当“当前物料”和“米”属于同一个计量单位组时才更新**。

```sql
UPDATE a
SET
    a.cComUnitCode   = b.cComUnitCode,   -- 主计量单位
    a.cShopUnit      = b.cComUnitCode,   -- 零售单位
    a.cPUComUnitCode = b.cComUnitCode,   -- 采购默认单位
    a.cSAComUnitCode = b.cComUnitCode,   -- 销售默认单位
    a.cSTComUnitCode = b.cComUnitCode,   -- 库存默认单位
    a.cCAComUnitCode = b.cComUnitCode,   -- 成本默认单位
    a.cModifyPerson  = 'admin',
    a.dModifyDate    = GETDATE()
FROM UFDATA_101_2018.dbo.Inventory a
INNER JOIN UFDATA_101_2018.dbo.ComputationUnit b
    ON b.cComUnitName = '米'
WHERE a.cInvCode = '3316010026'
  AND a.cGroupCode = b.cGroupCode;
```

先用这条检查一下是否同组、是否能改：

```sql
SELECT 
    a.cInvCode AS 存货编码,
    a.cInvName AS 存货名称,
    a.cGroupCode AS 当前单位组,
    a.cComUnitCode AS 当前主单位编码,
    cu.cComUnitName AS 当前主单位名称,
    b.cComUnitCode AS 米单位编码,
    b.cGroupCode AS 米单位组,
    b.cComUnitName AS 目标单位名称,
    CASE 
        WHEN a.cGroupCode = b.cGroupCode THEN '可以更新'
        ELSE '不能直接更新，单位组不同'
    END AS 校验结果
FROM UFDATA_101_2018.dbo.Inventory a
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu
    ON a.cComUnitCode = cu.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit b
    ON b.cComUnitName = '米'
WHERE a.cInvCode = '3316010026';
```

更新后复查：

```sql
SELECT 
    a.cInvCode AS 存货编码,
    a.cInvName AS 存货名称,
    cu1.cComUnitName AS 主计量单位,
    cu2.cComUnitName AS 零售单位,
    cu3.cComUnitName AS 采购单位,
    cu4.cComUnitName AS 销售单位,
    cu5.cComUnitName AS 库存单位,
    cu6.cComUnitName AS 成本单位
FROM UFDATA_101_2018.dbo.Inventory a
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu1 ON a.cComUnitCode   = cu1.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu2 ON a.cShopUnit      = cu2.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu3 ON a.cPUComUnitCode = cu3.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu4 ON a.cSAComUnitCode = cu4.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu5 ON a.cSTComUnitCode = cu5.cComUnitCode
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit cu6 ON a.cCAComUnitCode = cu6.cComUnitCode
WHERE a.cInvCode = '3316010026';
```

如果你要，我还可以直接给你补一版：**单位组不同也能处理的方案**。
user: 下次直接给我这版：
UPDATE UFDATA_101_2018.dbo.Inventory
SET
 cComUnitCode = b.cComUnitCode,
 cShopUnit = b.cComUnitCode
FROM UFDATA_101_2018.dbo.Inventory a
INNER JOIN UFDATA_101_2018.dbo.ComputationUnit b
 ON b.cComUnitName = '米'
WHERE a.cInvCode = '3316010026';


SELECT
 a.cInvCode AS 存货编码,
 a.cInvName AS 存货名称,
 a.cComUnitCode AS 主计量单位编码,
 b.cComUnitName AS 主计量单位名称,
 a.cShopUnit AS 零售单位编码
FROM UFDATA_101_2018.dbo.Inventory a
LEFT JOIN UFDATA_101_2018.dbo.ComputationUnit b
 ON a.cComUnitCode = b.cComUnitCode
WHERE a.cInvCode = '3316010026';
assistant: [[reply_to_current]] 记住了。  
下次遇到这种 **U8 存货单位修改**，我先直接给你这版，不再兜一大圈。
user: 我在UFDATA_105_2019运行UPDATE dbo.Inventory
SET
 cComUnitCode = b.cComUnitCode,
 cShopUnit = b.cComUnitCode
FROM UFDATA_101_2018.dbo.Inventory a
INNER JOIN dbo.ComputationUnit b
 ON b.cComUnitName = '米'
WHERE a.cInvCode = '3316010026';
为什么会有160413条物料的单位全部被更新了？

现在给出从UFDATA_101_2018中把所有物料的物料单位覆盖到UFDATA_105_2019的物料的sql语句
assistant: [[reply_to_current]] 因为你那条 SQL **没有把 105 的目标表和 101 的源表按物料编码关联起来**。

你当时在 `UFDATA_105_2019` 执行的是：

```sql
UPDATE dbo.Inventory
SET
 cComUnitCode = b.cComUnitCode,
 cShopUnit = b.cComUnitCode
FROM UFDATA_101_2018.dbo.Inventory a
INNER JOIN dbo.ComputationUnit b
 ON b.cComUnitName = '米'
WHERE a.cInvCode = '3316010026';
```

问题就在这里：

- `UPDATE dbo.Inventory` —— 真正被更新的是 **105账套里的整张 `dbo.Inventory`**
- `FROM UFDATA_101_2018.dbo.Inventory a` —— 这里只是拿了 **101账套** 的一条记录做条件
- 但 **没有写** `105.dbo.Inventory` 和 `101.dbo.Inventory a` 的关联条件，比如 `目标.cInvCode = a.cInvCode`

所以 SQL Server 实际上会把目标表 `UFDATA_105_2019.dbo.Inventory` 中**所有行**都参与更新，于是 160413 条全改了。

你现在要的是：

> **把 UFDATA_101_2018 中所有物料的单位，按物料编码覆盖到 UFDATA_105_2019**

直接用这版：

```sql
UPDATE tgt
SET
    tgt.cComUnitCode = src.cComUnitCode,
    tgt.cShopUnit    = src.cShopUnit
FROM UFDATA_105_2019.dbo.Inventory tgt
INNER JOIN UFDATA_101_2018.dbo.Inventory src
    ON tgt.cInvCode = src.cInvCode;
```

如果你想先看会改哪些，再执行，先跑这个：

```sql
SELECT
    tgt.cInvCode           AS 105存货编码,
    tgt.cInvName           AS 105存货名称,
    tgt.cComUnitCode       AS 105当前主单位编码,
    tgt.cShopUnit          AS 105当前零售单位编码,
    src.cComUnitCode       AS 101主单位编码,
    src.cShopUnit          AS 101零售单位编码
FROM UFDATA_105_2019.dbo.Inventory tgt
INNER JOIN UFDATA_101_2018.dbo.Inventory src
    ON tgt.cInvCode = src.cInvCode
WHERE ISNULL(tgt.cComUnitCode,'') <> ISNULL(src.cComUnitCode,'')
   OR ISNULL(tgt.cShopUnit,'')    <> ISNULL(src.cShopUnit,'');
```

如果你还想把 **采购/销售/库存/成本单位** 也一起从 101 覆盖到 105，我也可以直接给你一版完整覆盖 SQL。  
参考：`Inventory` 中单位相关字段包括 `cComUnitCode`、`cShopUnit`、`cPUComUnitCode`、`cSAComUnitCode`、`cSTComUnitCode`、`cCAComUnitCode`。Source: `memory/u8-docs/openclaw学习资料/U8相关sql/20241204-物料相关.md#L1-L52`, `memory/u8-docs/openclaw学习资料/U8相关sql/20241204-物料相关.md#L154-L193`
