# Session: 2026-05-13 14:19:16 GMT+8

- **Session Key**: agent:main:main
- **Session ID**: e53fa819-da25-4ba2-a6f3-7c832d4fa6c7
- **Source**: webchat

## Conversation Summary

user: [Wed 2026-05-13 14:08 GMT+8] 给出查询U8到货单号：20200724009及其表体的sql语句
assistant: 直接用这两段就行。你这个单号看起来是 **采购到货单号 `cCode`**，表头在 `PU_ArrivalVouch`，表体在 `PU_ArrivalVouchs`。Source: memory/u8-docs/openclaw学习资料/U8相关sql/20241214-采购到货及入库明细摸底.md#L1-L35

**1）查到货单表头**
```sql
SELECT *
FROM UFDATA_101_2018.dbo.PU_ArrivalVouch
WHERE cCode = '20200724009';
```

**2）查该到货单对应表体**
```sql
SELECT b.*
FROM UFDATA_101_2018.dbo.PU_ArrivalVouch a
INNER JOIN UFDATA_101_2018.dbo.PU_ArrivalVouchs b
    ON a.ID = b.ID
WHERE a.cCode = '20200724009';
```

如果你想**表头+表体一起看**，我更建议用这个：
```sql
SELECT
    a.cCode      AS 到货单号,
    a.dDate      AS 到货日期,
    a.cVenCode   AS 供应商编码,
    b.AutoID     AS 表体AutoID,
    b.cInvCode   AS 存货编码,
    b.iQuantity  AS 数量,
    b.iUnitCost  AS 单价,
    b.iMoney     AS 金额,
    b.iSum       AS 价税合计,
    b.iTaxRate   AS 税率
FROM UFDATA_101_2018.dbo.PU_ArrivalVouch a
INNER JOIN UFDATA_101_2018.dbo.PU_ArrivalVouchs b
    ON a.ID = b.ID
WHERE a.cCode = '20200724009';
```

如果你不是 **101账套**，把 `UFDATA_101_2018` 改成你的实际账套库名即可。  
如果你要，我也可以顺手给你补一版：**关联存货档案名称/规格型号** 的完整 SQL。
