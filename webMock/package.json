{
  "name": "onlyweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host ${HOST:-0.0.0.0} --port ${WEBMOCK_PORT:-18475}",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host ${HOST:-0.0.0.0} --port ${WEBMOCK_PREVIEW_PORT:-18476}"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3"
  }
}
